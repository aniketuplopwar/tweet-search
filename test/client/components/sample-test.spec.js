let expect = require('expect'); //my root-test lives in components/__tests__/, so this is how I require in my components.

describe('App', function () {
  it('renders without problems', function () {

    expect(true).toBe(true);
  });
});